const _ = require('lodash')

module.exports = function ({ fastify }) {
  // setInterval(() => {
  //   fastify.mongo.db.collection('flow').insertOne({
  //     ts: new Date(),
  //     value: _.random(99)
  //   })
  // }, 1000)
}
